git config --global user.name "actona"
git config --global user.email "actonaputra@gmail.com"

git init

git status (gunanya untuk mengecek status dari file folder git)

git add index.html (index.html berupa file di folder)

git add --all (mirip git .add, bedanya --all langsung semua staging environment)

git commit -m "First release of Hello World!" (-m adalah pesan)

git commit -a -m "Updated index.html with a new line" (-a adalah untuk update stage and commit)

git log